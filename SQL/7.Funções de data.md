## 41. **Obter a Data Atual**

   ```sql
   SELECT CURRENT_DATE() AS data_atual;
   ```

   - **Descrição:** Retorna a data atual do sistema.
   - **Exemplo:**  
     ```sql
     SELECT CURRENT_DATE() AS data_atual;
     ```

---

## 42. **Obter Data e Hora Atual**

   ```sql
   SELECT NOW() AS data_hora_atual;
   ```

   - **Descrição:** Retorna a data e hora atual do sistema.
   - **Exemplo:**  
     ```sql
     SELECT NOW() AS data_hora_atual;
     ```

---

## 43. **Extrair o Ano de uma Data**

   ```sql
   SELECT YEAR(coluna_data) AS ano
   FROM nome_da_tabela;
   ```

   - **Descrição:** Extrai o ano de uma coluna do tipo `DATE` ou `DATETIME`.
   - **Exemplo:**  
     ```sql
     SELECT YEAR(data_contratacao) AS ano_contratacao 
     FROM funcionarios;
     ```

---

## 44. **Extrair o Mês de uma Data**

   ```sql
   SELECT MONTH(coluna_data) AS mes
   FROM nome_da_tabela;
   ```

   - **Descrição:** Extrai o mês de uma coluna do tipo `DATE` ou `DATETIME`.
   - **Exemplo:**  
     ```sql
     SELECT MONTH(data_contratacao) AS mes_contratacao 
     FROM funcionarios;
     ```

---

## 45. **Extrair o Dia de uma Data**

   ```sql
   SELECT DAY(coluna_data) AS dia
   FROM nome_da_tabela;
   ```

   - **Descrição:** Extrai o dia do mês de uma coluna do tipo `DATE` ou `DATETIME`.
   - **Exemplo:**  
     ```sql
     SELECT DAY(data_contratacao) AS dia_contratacao 
     FROM funcionarios;
     ```

---

## 46. **Obter o Nome do Mês de uma Data**

   ```sql
   SELECT MONTHNAME(coluna_data) AS nome_mes
   FROM nome_da_tabela;
   ```

   - **Descrição:** Retorna o nome do mês de uma coluna do tipo `DATE` ou `DATETIME`.
   - **Exemplo:**  
     ```sql
     SELECT MONTHNAME(data_contratacao) AS nome_mes 
     FROM funcionarios;
     ```

---

## 47. **Obter o Dia da Semana de uma Data**

   ```sql
   SELECT DAYNAME(coluna_data) AS nome_dia
   FROM nome_da_tabela;
   ```

   - **Descrição:** Retorna o nome do dia da semana de uma coluna do tipo `DATE` ou `DATETIME`.
   - **Exemplo:**  
     ```sql
     SELECT DAYNAME(data_contratacao) AS dia_semana 
     FROM funcionarios;
     ```

---

## 48. **Adicionar Dias a uma Data**

   ```sql
   SELECT DATE_ADD(coluna_data, INTERVAL quantidade_dias DAY) AS nova_data
   FROM nome_da_tabela;
   ```

   - **Descrição:** Adiciona um número específico de dias a uma data.
   - **Exemplo:**  
     ```sql
     SELECT DATE_ADD(data_contratacao, INTERVAL 30 DAY) AS nova_data 
     FROM funcionarios;
     ```

---

## 49. **Subtrair Dias de uma Data**

   ```sql
   SELECT DATE_SUB(coluna_data, INTERVAL quantidade_dias DAY) AS nova_data
   FROM nome_da_tabela;
   ```

   - **Descrição:** Subtrai um número específico de dias de uma data.
   - **Exemplo:**  
     ```sql
     SELECT DATE_SUB(data_contratacao, INTERVAL 10 DAY) AS nova_data 
     FROM funcionarios;
     ```

---

## 50. **Calcular a Diferença Entre Duas Datas**

   ```sql
   SELECT DATEDIFF(data_fim, data_inicio) AS diferenca_dias
   FROM nome_da_tabela;
   ```

   - **Descrição:** Calcula o número de dias entre duas datas.
   - **Exemplo:**  
     ```sql
     SELECT DATEDIFF(CURRENT_DATE(), data_contratacao) AS dias_trabalhados 
     FROM funcionarios;
     ```

---

## 51. **Formatação de Datas**

   ```sql
   SELECT DATE_FORMAT(coluna_data, '%d/%m/%Y') AS data_formatada
   FROM nome_da_tabela;
   ```

   - **Descrição:** Formata uma data em um formato específico, como `dd/mm/yyyy`.
   - **Exemplo:**  
     ```sql
     SELECT DATE_FORMAT(data_contratacao, '%d/%m/%Y') AS data_formatada 
     FROM funcionarios;
     ```

---

## 52. **Obter o Último Dia do Mês de uma Data**

   ```sql
   SELECT LAST_DAY(coluna_data) AS ultimo_dia_mes
   FROM nome_da_tabela;
   ```

   - **Descrição:** Retorna o último dia do mês de uma data específica.
   - **Exemplo:**  
     ```sql
     SELECT LAST_DAY(data_contratacao) AS ultimo_dia_mes 
     FROM funcionarios;
     ```

---

## 53. **Obter o Primeiro Dia do Mês de uma Data**

   ```sql
   SELECT DATE_FORMAT(coluna_data, '%Y-%m-01') AS primeiro_dia_mes
   FROM nome_da_tabela;
   ```

   - **Descrição:** Retorna o primeiro dia do mês de uma data específica.
   - **Exemplo:**  
     ```sql
     SELECT DATE_FORMAT(data_contratacao, '%Y-%m-01') AS primeiro_dia_mes 
     FROM funcionarios;
     ```